<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DarkBox"
        tools:targetApi="31">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DarkBox">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DarkBox" />

        <activity
            android:name=".InventoryActivity"
            android:exported="true"
            android:label="Inventario"
            android:theme="@style/Theme.DarkBox" />

        <activity
            android:name=".IngresarEquipoActivity"
            android:exported="true"
            android:label="Ingresar Equipo"
            android:theme="@style/Theme.DarkBox" />

        <activity
            android:name=".VerInventarioActivity"
            android:exported="true"
            android:label="Ver Inventario"
            android:theme="@style/Theme.DarkBox" />

        <activity
            android:name=".ActualizarEstadoActivity"
            android:exported="true"
            android:label="Actualizar Estado"
            android:theme="@style/Theme.DarkBox" />

        <!-- Agregar ClientesActivity -->
        <activity
            android:name=".ClientesActivity"
            android:exported="true"
            android:label="Clientes"
            android:theme="@style/Theme.DarkBox" />

        <activity
            android:name=".IngresarClienteActivity"
            android:exported="true"
            android:label="Ingresar Cliente"
            android:theme="@style/Theme.DarkBox" />


        <!-- Agregar AgendaActivity -->
        <activity
            android:name=".AgendaActivity"
            android:exported="true"
            android:label="Agenda"
            android:theme="@style/Theme.DarkBox" />

        <!-- Agregar SolicitudInstalacionActivity -->
        <activity
            android:name=".SolicitudInstalacionActivity"
            android:exported="true"
            android:label="Solicitud Instalación"
            android:theme="@style/Theme.DarkBox" />

        <activity
            android:name=".OtrasGestionesActivity"
            android:exported="true"
            android:label="Otras Gestiones"
            android:theme="@style/Theme.DarkBox" />


        <!-- Agregar CrearAgendaActivity -->
        <activity
            android:name=".CrearAgendaActivity"
            android:exported="true"
            android:label="Crear Agenda"
            android:theme="@style/Theme.DarkBox" />

        <!-- Agregar VerAgendaActivity -->
        <activity
            android:name=".VerAgendaActivity"
            android:exported="true"
            android:label="Ver Agenda"
            android:theme="@style/Theme.DarkBox" />

        <activity
            android:name=".VerClienteActivity"
            android:exported="true"
            android:label="Ver Cliente"
            android:theme="@style/Theme.DarkBox" />

        <activity
            android:name=".InformesActivity"
            android:exported="true"
            android:label="Informes"
            android:theme="@style/Theme.DarkBox" />

        <activity
            android:name=".CredencialesActivity"
            android:exported="true"
            android:label="Credenciales"
            android:theme="@style/Theme.DarkBox" />

        <activity
            android:name=".AccessActivity"
            android:exported="true"
            android:label="Perfiles de Acceso"
            android:theme="@style/Theme.DarkBox" />

        <activity
            android:name=".EditAccesActivity"
            android:windowSoftInputMode="adjustResize|stateHidden"
            android:exported="true"
            android:label="Editar Credencial"
            android:theme="@style/Theme.DarkBox" />

        <activity
            android:name=".TiketsActivity"
            android:exported="true"
            android:label="Tickets"
            android:theme="@style/Theme.DarkBox" />

        <activity
            android:name=".CrearSolicitudActivity"
            android:exported="true"
            android:label="Crear Solicitud"
            android:theme="@style/Theme.DarkBox" />


    </application>
</manifest>
