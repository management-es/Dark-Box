<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".LoadingActivity">

    <!-- Lottie Animation for Loading -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/loading_animation"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        app:lottie_autoPlay="true"
        app:lottie_loop="true"
        app:lottie_rawRes="@raw/loading" />

    <!-- Lottie Animation for Loading2, bigger size and below the first animation -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/loading2_animation"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_below="@id/loading_animation"
        android:layout_centerHorizontal="true"
        app:lottie_autoPlay="true"
        app:lottie_loop="true"
        app:lottie_rawRes="@raw/loading2" />

    <!-- Error message for no connection -->
    <TextView
        android:id="@+id/error_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/loading2_animation"
        android:layout_centerHorizontal="true"
        android:visibility="gone"
        android:text="Sin conexión a internet"
        android:textColor="#FF0000"
        android:textSize="18sp" />

</RelativeLayout>
