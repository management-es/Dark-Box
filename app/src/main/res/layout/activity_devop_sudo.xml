<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Título de la página -->
    <TextView
        android:id="@+id/tvDevOpSuDoTitulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Informes y Solicitudes"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginLeft="16dp"/>

    <!-- TextView para la cantidad de informes pendientes -->
    <TextView
        android:id="@+id/tvCantidadInformesPendientes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Informes Pendientes: 0"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/tvDevOpSuDoTitulo"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginLeft="16dp"/>

    <!-- RecyclerView para mostrar los informes -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewInformes"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/tvCantidadInformesPendientes"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginTop="16dp" />
</androidx.constraintlayout.widget.ConstraintLayout>
